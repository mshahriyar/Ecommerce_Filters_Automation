[pytest]
# More verbose output so failures show reasons in the terminal.
addopts = -vv -s -v -rA --html=reports/report.html 
markers =
    morefilters: Tests related to More Filters functionality
    address: Tests related to address management